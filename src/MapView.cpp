#include <iostream>

#include "MapView.hpp"

//void MapView::onMousePress(int x, int y)
//{
//	if (x < 0 || y < 0 || x >= VIEW_WIDTH || y >= VIEW_HEIGHT)
//		return;
//
//	for (auto& country : countries) {
//		if (country.collides(x, y)) {
//			std::cerr << "MapView::onMousePress x: " << x << ", y: " << y << ", collides with: " << country.getName() << std::endl;
//			break;
//		}
//	}
//}
